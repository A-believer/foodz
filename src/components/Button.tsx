import React from 'react'

type Props = {}

const Button = (props: Props) => {
  return (
    <button className='px-5 py-2.5 rounded-lg uppercase bg-green text-white font-medium'>Order Now</button>
  )
}

export default Button